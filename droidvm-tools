#!/bin/bash
# Wrapper script for droidvm-tools CLI
# This allows running "droidvm-tools" instead of "uv run droidvm-tools"

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
uv run droidvm-tools "$@"

# DroidVM Tools Configuration
# Copy this file to .env and modify as needed

# Server Configuration
DROIDVM_HOST=0.0.0.0
DROIDVM_PORT=8000
DROIDVM_RELOAD=false

# Set to true for development mode with auto-reload
# DROIDVM_RELOAD=true

# Network Configuration
# If running on specific network interface, set the host accordingly
# For local only: DROIDVM_HOST=127.0.0.1
# For network access: DROIDVM_HOST=0.0.0.0
